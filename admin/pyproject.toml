[project]
name = "linux-admin"
version = "0.1.0"
description = "Linux administration scripts and utilities"
requires-python = ">=3.10"
dependencies = [
    "websockets>=12.0",
]

[project.optional-dependencies]
dev = []
