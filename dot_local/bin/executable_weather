#!/usr/bin/env bash

if [ -z "$WEATHER_LAT" ] || [ -z "$WEATHER_LON" ]; then
    echo "Set WEATHER_LAT and WEATHER_LON in ~/.config/zsh/zshrc.local"
    exit 1
fi

rustormy --lat "$WEATHER_LAT" --lon "$WEATHER_LON" --colors -u imperial
